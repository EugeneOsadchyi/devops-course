pipeline {
  agent any

  stages {
    stage('Clone repository') {
      steps {
        script {
          checkout scm
        }
      }
    }

    stage('Apply Kubernetes files') {
      steps {
        sh 'kubectl apply -f ./k8s'
      }
    }
  }
}
